# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml

# JS
node_modules/

# The gates workflow checks colormath out here inside consumer repos; make
# sure a local experiment never commits it.
.colormath-lib/

# Leftover skill-creator eval scratch from before the plugin moved to
# ColorMath/skills. The directories are untracked local artifacts, not something
# this repo ships; keep ignoring them so removing the plugin does not sweep them
# into a commit.
plugin/skills/*/evals/
plugin/skills/qa-workspace/
